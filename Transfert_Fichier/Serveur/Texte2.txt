ertyuiop//tyuiop//^ertyuiop=>Texte58.txt
ertyuio//retyuio//ertyuiortyuio//wertyuiop=>Texte89.txt
find / (-usr idealisoa -a perm 666 -print) | wc -l////4)//find / -name "core" -print -ok rm {}\;////Exercice 2//>Dossier1// chmod 751 Dossier1// chmod u=rwx,g=rx,o=x Dossier1//>Dossier2 // chmod 521 Dossier2// chmod u=rx,g=w,o=x Dossier2////Exercice 3=>Texte52.txt
select * from rallye where datefin-datedebut>=10;////3 avant dernier enregistrement//select * from rallye order by idrallye desc limit 1,3;////distance speciale desc par rallye//select r.nomrallye, sum(s.distance) as somme from speciale s //join rallye r on s.idrallye=r.idrallye //group by r.idrallye order by somme asc;////rallye mverina >2//select equipage, count(equipage) as c from equipage //group by equipage having c>=2 order by c;////select equipage, count(equipage) as c //from equipage group by equipage order by c desc;////create or replace view v_equi_ral as//(select sum(t.temps) as total, e.equipage, count(s.idspeciale) as nbs, c.nomclasse=>Texte48.txt